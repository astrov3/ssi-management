{
    "name": "ssi-identity-manager-testing",
    "version": "1.0.0",
    "description": "Comprehensive E2E testing suite for SSI Identity Manager",
    "main": "index.js",
    "scripts": {
        "test:all": "bash scripts/run-all-tests.sh",
        "test:all-win": "scripts\\run-all-tests.bat",
        "test:generate-data": "node scripts/generate-test-data.js",
        "test:performance": "node scripts/performance-test.js",
        "test:manual": "node scripts/manual-testing-checklist.js",
        "test:report": "node scripts/generate-test-report.js",
        "test:smart-contract": "cd ssi-smart-contract && npm test",
        "test:frontend": "cd ssi-frontend && npm test",
        "setup:all": "npm run setup:smart-contract && npm run setup:frontend",
        "setup:smart-contract": "cd ssi-smart-contract && npm install",
        "setup:frontend": "cd ssi-frontend && npm install",
        "build:all": "npm run build:smart-contract && npm run build:frontend",
        "build:smart-contract": "cd ssi-smart-contract && npx hardhat compile",
        "build:frontend": "cd ssi-frontend && npm run build",
        "deploy:contract": "cd ssi-smart-contract && npx hardhat run scripts/deploy.js --network sepolia",
        "clean": "npm run clean:reports && npm run clean:temp",
        "clean:reports": "del /Q *.json *.md 2>nul || rm -f *.json *.md 2>/dev/null || echo 'No files to clean'",
        "clean:temp": "del /Q temp_*.* 2>nul || rm -f temp_*.* 2>/dev/null || echo 'No temp files to clean'",
        "docs:generate": "node -e \"console.log('📚 Documentation generation complete. Check USER_GUIDE.md and E2E_TESTING_GUIDE.md')\""
    },
    "keywords": [
        "blockchain",
        "identity",
        "verifiable-credentials",
        "testing",
        "e2e",
        "ethereum"
    ],
    "author": "SSI Development Team",
    "license": "MIT",
    "devDependencies": {
        "@faker-js/faker": "^8.0.0",
        "chalk": "^4.1.0",
        "hardhat": "^3.0.7"
    },
    "engines": {
        "node": ">=16.0.0",
        "npm": ">=7.0.0"
    },
    "repository": {
        "type": "git",
        "url": "."
    },
    "directories": {
        "scripts": "./scripts",
        "docs": "./docs"
    },
    "config": {
        "test-data-count": {
            "organizations": 15,
            "credentials": 75,
            "accounts": 4
        }
    },
    "dependencies": {
        "react": "^19.2.0",
        "react-dom": "^19.2.0",
        "vite": "^7.1.10"
    }
}
